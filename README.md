# you must have latest node & npm installe. If not, please install

### Run command in root directory:
`npm install`
# then

`npm start`


### After this, you will be able to run project on localhost:3000